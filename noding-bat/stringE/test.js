const chai = require("chai");
const assert = chai.assert;
const stringE = require("./stringE");

describe("A program that will return true if contains between 1 and 3 'e' characters.", () => {
  it("Should return true if string has between 1 and 3 'e' characters", () => {
    
  });
});
